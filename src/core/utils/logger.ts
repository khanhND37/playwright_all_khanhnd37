export const printLog = (msg: string) => {
  // eslint-disable-next-line no-console
  console.log(msg);
};

export const printTableLog = msg => {
  // eslint-disable-next-line no-console
  console.table(msg);
};
