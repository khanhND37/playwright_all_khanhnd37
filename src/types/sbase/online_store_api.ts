export type DefaultVariantSettings = {
  none_default_variant: boolean;
  exclude_options: {
    enable: boolean;
    list_options_exclude: Array<string>;
  };
};
export type FacebookConversions = {
  id: string;
  token: string;
};
